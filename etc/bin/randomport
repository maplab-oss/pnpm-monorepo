#!/bin/bash
# Generate a random 4-digit port number in a safe range (1024-9999)
# Avoids well-known ports (0-1023) while staying in 4-digit range

# Generate a random port between 1024 and 9999
PORT=$((1024 + RANDOM % 8976))

echo $PORT
